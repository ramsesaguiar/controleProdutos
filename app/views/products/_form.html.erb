<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br />
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.label :preco %><br />
    <%= f.text_field :preco %>
  </div>
  <div class="field">
    <%= f.label :data %><br />
    <%= f.datetime_select :data, :start_year => 1970 %>
  </div>
  <div class="field">
    <%= f.label :descricao %><br />
    <%= f.text_area :descricao, :rows =>5, :cols=> 40 %>
  </div>
   
   <div class="field">
    <%= f.label :categoria %><br />
    <%= collection_select(:product ,:category_id,  @categoria,  :id, :titulo, :selected => @padrao ) %>
  </div>
  
<div class="actions">
    <%= f.submit 'Criar' %>
  </div>
<% end %>

